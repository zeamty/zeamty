# ssh生成密钥
# ssh-keygen -o -t rsa -C "${email}" -b 4096
# 拷贝公钥到远程机器
# ssh-copy-id -i ~/.ssh/id_rsa.pub ${username}@${ip}

# 安装homebrew，配置国内源以加速下载
# https://developer.aliyun.com/mirror/homebrew/

# 官网安装：Chrome、Sublime Text、VSCode
